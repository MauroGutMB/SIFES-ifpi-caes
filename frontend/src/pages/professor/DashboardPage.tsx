import { Typography } from '@mui/material';

export function ProfessorDashboardPage() {
  return (
    <>
      <Typography variant="h4" gutterBottom>
        Painel do professor
      </Typography>
      <Typography color="text.secondary">
        Em construção — os widgets deste painel entram na próxima etapa.
      </Typography>
    </>
  );
}
